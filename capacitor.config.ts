import type { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'io.ionic.starter',
  appName: 'SeminarioMobile-AppDeNoticias',
  webDir: 'www'
};

export default config;
